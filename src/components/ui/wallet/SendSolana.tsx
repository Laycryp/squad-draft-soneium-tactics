// src/components/ui/wallet/SendSolana.tsx
"use client";

import React from "react";

export type SendSolanaProps = {
  to?: string;
};

/**
 * Stub component for the old Neynar wallet demo (SendSolana).
 * Not needed in the Squad Draft mini app, so we return null.
 */
export function SendSolana(_props: SendSolanaProps) {
  return null;
}

export default SendSolana;
